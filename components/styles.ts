import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
    currencyStyle: {
      borderColor: "#111", 
      borderWidth: 1,
      paddingLeft: 45,
      paddingRight: 45,
      margin: 10
    }
  });